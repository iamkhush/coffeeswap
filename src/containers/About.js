import React from 'react';
import ReactDOM from 'react-dom';
import {AboutPage, Header, Footer} from 'components';

class About extends React.Component{
    render() {
        return (
                <AboutPage />
        );
  }  
}

export default About;