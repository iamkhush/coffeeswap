import React from 'react';
import ReactDOM from 'react-dom';
import {IndexPage, Header, Footer} from 'components';

class Home extends React.Component{
  render() {
    return (
        <IndexPage />
    );
  }  
}

export default Home;